export default class ActionElement {

    constructor(container, model, data) {
        this.container = container;
        this.model = model;
        this.data = data;
    }

    do() {}
    undo() {}
}