<div class="{{cssClass}}" data-widget="{{widget}}" data-type="{{type}}" data-id="{{id}}">
    <div class="widget">
        <div class="b1">
            <img src="{{{icon}}}" class="img-fluid drag-item" /> 
        </div>
        <div class="b2" data-prev>
            <span>{{label}}</span>
        </div>
        <div class="b3" data-toolbar>
            <button class="author-add-content btn btn-sm btn-success" data-toggle="tooltip" 
                title="{{translate "items.add"}}">
                <i class="fa fa-plus-circle"></i>
            </button>
            {{#if canEdit}}
            <button class="author-edit-element btn btn-sm btn-success" data-toggle="tooltip"
                title="{{translate "items.edit"}}">
                <i class="fa fa-edit"></i>
            </button>
            {{/if}}
            {{#if canCopy}}
            <button class="author-copy-element btn btn-sm btn-success" data-toggle="tooltip"
                title="{{translate "items.duplicate"}}">
                <i class="fa fa-copy"></i>
            </button>
            <button class="author-export-element btn btn-sm btn-success" data-toggle="tooltip"
                title="{{translate "items.export"}}">
                <i class="fa fa-file-export"></i>
            </button>
            {{/if}}
            <button class="author-remove-element btn btn-sm btn-danger" data-toggle="tooltip"
                title="{{translate "items.delete"}}">
                <i class="fa fa-times"></i>
            </button>
        </div>
    </div>
    <div data-widget="{{widget}}" data-type="{{type}}" class="element-container dragula-container" data-content></div>
</div>