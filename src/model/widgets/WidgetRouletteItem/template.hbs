<div class="container-item">
    <div class="{{cssClass}}" data-widget="{{widget}}" data-id="{{id}}" type="container">
        <div class="widget row flex-column flex-md-row no-gutters">
            <div class="b1 col-auto d-none d-md-inline-block">
                <img loading="lazy" src="{{{icon}}}" class="img-fluid drag-item" alt="" /> 
            </div>
            <div class="b2 col-12 col-md-8" data-prev>{{{label}}}</div>
            <div class="b3 col-12 col-md-auto ml-auto" data-toolbar>
                <button type="button" class="author-bank-questions btn btn-sm btn-success" 
                    data-toggle="tooltip" title="{{ translate "widgets.QuestionsBank.label" }}">
                    <i class="fa fa-file-import"></i>
                </button>
                {{#if canAdd}}
                <button type="button" class="author-add-content btn btn-sm btn-success" 
                    data-toggle="tooltip" title="{{translate "items.add"}}">
                    <i class="fa fa-plus-circle"></i>
                </button>
                {{/if}}
                {{#if canEdit}}
                <button type="button" class="author-edit-element btn btn-sm btn-success" 
                    data-toggle="tooltip" title="{{translate "items.edit"}}">
                    <i class="fa fa-edit"></i>
                </button>
                {{/if}}                
                {{#if canCopy}}
                <button type="button" class="author-copy-element btn btn-sm btn-success" 
                    data-toggle="tooltip" title="{{translate "items.duplicate"}}">
                    <i class="fa fa-copy"></i>
                </button>
                <button type="button" class="author-export-element btn btn-sm btn-success" 
                    data-toggle="tooltip" title="{{translate "items.export"}}">
                    <i class="fa fa-file-export"></i>
                </button>
                {{/if}}
                {{#if canDelete}}
                <button type="button" class="author-remove-element btn btn-sm btn-danger" 
                    data-toggle="tooltip" title="{{translate "items.delete"}}">
                    <i class="fa fa-times"></i>
                </button>
                {{/if}}
            </div>
        </div>
        <div data-widget="{{widget}}" class="element-container dragula-container" data-content>
            {{{children}}}
        </div>
    </div>
</div>